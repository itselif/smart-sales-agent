stages:
  - deploy
  - build
  - test

build-and-deploy-server:
  stage: deploy
  image: docker:stable
  services:
    - docker:dind
  script:
    - cp $RSA_PRV_KEY  keys/rsa.key
    - sudo bash -c "docker build -f Dockerfile -t reporting-service:latest ."
    - sudo bash -c 'cd undefined/salesai/  && docker compose up -d reporting-service'
  only:
    - main
# Notice: This file is generated by Mindbricks and formatted by prettier-plugin-yaml
